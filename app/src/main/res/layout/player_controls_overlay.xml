<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:id="@+id/controlsTopBar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#66000000"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBack"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            app:backgroundTint="@color/player_overlay_button_bg"
            android:text="Back"
            android:textAllCaps="false"
            android:textColor="@android:color/white" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/tvTitleCenter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSbs"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:insetLeft="8dp"
            android:insetRight="8dp"
            app:backgroundTint="@color/player_sbs_button_bg"
            android:text="OU➤SBS"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:checkable="true" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnShift"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:insetLeft="8dp"
            android:insetRight="8dp"
            app:backgroundTint="@color/player_overlay_button_bg"
            android:text="Shift"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:checkable="true" />

        <ImageButton
            android:id="@+id/btnAudio"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/player_icon_button_bg"
            android:backgroundTint="@color/player_overlay_button_bg"
            android:contentDescription="@string/select_audio_track"
            android:scaleType="centerInside"
            android:src="@drawable/ear_sound_24px"
            android:tint="@android:color/white" />
    </LinearLayout>

    <!-- Fullscreen tap-to-dismiss root for custom audio menu -->
    <FrameLayout
        android:id="@+id/audioMenuRoot"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone">

        <!-- Centered menu for non-SBS mode -->
        <LinearLayout
            android:id="@+id/audioMenuCenter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:background="#CC000000"
            android:padding="12dp" />

        <!-- Left panel for SBS mode -->
        <LinearLayout
            android:id="@+id/audioMenuLeft"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|center_vertical"
            android:layout_marginStart="24dp"
            android:orientation="vertical"
            android:background="#CC000000"
            android:padding="12dp"
            android:visibility="gone" />

        <!-- Right panel for SBS mode -->
        <LinearLayout
            android:id="@+id/audioMenuRight"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="24dp"
            android:orientation="vertical"
            android:background="#CC000000"
            android:padding="12dp"
            android:visibility="gone" />
    </FrameLayout>

</merge>
